FROM python:3.11-slim
WORKDIR /app
RUN apt-get update && apt-get install -y --no-install-recommends gcc && rm -rf /var/lib/apt/lists/*
COPY chambers-sim/ .
RUN pip install --no-cache-dir -e ".[dev]"
ENTRYPOINT ["chambers-sim"]
CMD ["demo", "--vehicles", "10", "--duration", "60"]
